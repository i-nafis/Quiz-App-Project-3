<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error</title>
</head>
<body>
  <div class="container">
    <h1>An Error Occurred</h1>
    <h2><%= (typeof error !== 'undefined' && error.message) ? error.message : 'Something went wrong.' %></h2>

    <% if (typeof error !== 'undefined' && error.status) { %>
      <p>Status Code: <%= error.status %></p>
    <% } %>

    <% if (typeof error !== 'undefined' && error.stack) { %>
      <pre><%= error.stack %></pre>
    <% } %>

    <a href="/">Return Home</a>
  </div>
</body>
</html>