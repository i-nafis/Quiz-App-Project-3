<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/quiz.css">
</head>
<body>
  <% include partials/header %>
  
  <div class="results-container">
    <h1><%= title %></h1>
    <p>You scored <strong><%= score %></strong> out of <strong><%= total %></strong>.</p>
    <p class="feedback"><%= message %></p>
    
    <!-- Display additional detailed results (optional) -->
    <div class="result-summary">
      <h1>Your Score: <%= score %> out of <%= total %></h1>
      <p>
        <% if (score === total) { %>
          Perfect score! Excellent work!
        <% } else if (score / total >= 0.8) { %>
          Well done, you did a great job!
        <% } else if (score / total >= 0.5) { %>
          Not bad, but you need to know more.
        <% } else { %>
          Better luck next time! Keep studying and try again.
        <% } %>
      </p>
      <div class="actions">
        <a href="/quiz" class="btn">Play Again</a>
        <a href="/leaderboard" class="btn">Leaderboard</a>
      </div>
    </div>
    <script src="/js/home.js"></script>
</body>
</html>
