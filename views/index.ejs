<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta2/css/all.min.css"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <%- include('partials/header') %>
      <div class="container">
        <div class="home-container">
          <h1>Are You Ready?</h1>
          <p class="tagline">Challenge yourself with our knowledge quiz!</p>
        </div>

          <% if (user) { %>
          <div class="dropdown-wrapper" style="display: flex; justify-content: center; margin-top: 0.5rem; margin-bottom: 1.5rem;">
            <form action="/quiz/start" method="POST" class="start-form" id="quizForm" style="display: flex; align-items: center; gap: 1rem;">
              <label for="numQuestions" style="font-size: 1.5rem; color: white;">Number of Questions:</label>
              <select name="numQuestions" id="numQuestions" required style="padding: 0.5rem 1rem; font-size: 1.2rem; border-radius: 10px;">
                <option value="5">5</option>
                <option value="10" selected>10</option>
                <option value="15">15</option>
                <option value="20">20</option>
              </select>
            </form>
          </div>
          <% } %>

          <div class="action-buttons">
            <% if (user) { %>
              <button type="submit" form="quizForm" class="btn">
                <i class="fas fa-play-circle"></i> Start Quiz
              </button>
              <a href="/leaderboard" class="btn secondary-btn">
                <i class="fas fa-trophy"></i> Leaderboard
              </a>
            <% } else { %>
              <a href="/auth/login" class="btn login-btn">
                <i class="fas fa-sign-in-alt"></i> Sign In
              </a>
              <a href="/leaderboard" class="btn secondary-btn">
                <i class="fas fa-trophy"></i> Leaderboard
              </a>
            <% } %>
          </div>

          <!-- <div class="features">
            <div class="feature">
              <i class="fas fa-question-circle"></i>
              <h3>Challenging Questions</h3>
              <p>Test your knowledge with our curated quiz questions</p>
            </div>
            <div class="feature">
              <i class="fas fa-stopwatch"></i>
              <h3>Beat the Clock</h3>
              <p>Race against time to improve your score</p>
            </div>
            <div class="feature">
              <i class="fas fa-trophy"></i>
              <h3>Compete & Win</h3>
              <p>Earn your spot on our global leaderboard</p>
            </div> -->
          </div>
        </div>
    </div>
    <script src="/js/home.js"></script>
  </body>
</html>
