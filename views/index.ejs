<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || "Test Your Knowledge" %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('partials/header') %>

  <div class="container">
    <div id="home" class="flex-column flex-center">
      <h1>Test Your Knowledge</h1>
      <% if (user) { %>
        <!-- If user is logged in, show the Play button -->
        <a href="/quiz" class="btn">Play</a>
      <% } else { %>
        <!-- If user is not logged in, show Sign In and Register buttons -->
        <a href="/auth/login" class="btn">Sign In</a>
        <a href="/auth/register" class="btn">Register</a>
      <% } %>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
